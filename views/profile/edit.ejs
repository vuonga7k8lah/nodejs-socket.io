<!DOCTYPE html>
<html lang="en">
<%- include ("../partials/header") %>

    <body>
        <%- include ("../partials/navbar") %>
            <div class="container">
                <div class="mt-4">
                    <h2 style="text-align: center;">Update</h2>
                    <form action="/me/update/<%= data._id%>?_method=PUT" method="POST" enctype="multipart/form-data">
                        <div class="mb-3 row">
                            <label for="name" class="col-sm-2 col-form-label">Name</label>
                            <div class="col-sm-10">
                                <input type="text" id="edit_name" name="edit_name" value="<%= data.Name%>" class="form-control" id="name">
                            </div>
                        </div>
                        <div class="mb-3 row">
                            <label for="level" class="col-sm-2 col-form-label">Level</label>
                            <div class="col-sm-10">
                                <input type="number" id="edit_level" name="edit_level" value="<%= data.Level%>" class="form-control" id="level">
                            </div>
                        </div>
                        <!-- <div class="mb-3">
                            <label for="formFile" class="form-label">Upload</label>
                            <input class="form-control" name="image" type="file" id="formFile">
                            <div class="col-4 mt-4">
                                <img src="/upload/<%= data.Image%>" alt="<%= data.Name%>" style="height: 200px;width: 200px;">
                            </div>
                        </div> -->
                        <input type="submit" value="Lưu Lại">
                    </form>
                </div>
            </div>
            <%- include ("../partials/footer") %>
    </body>

</html>